# Webhook 上传设置指南（用户向）

本页教你把 Gadgetbridge 里的健康数据上传到自己的服务器（AstrBot + health_monitor 插件），
之后就可以用聊天机器人查数据、收异常告警。

> 本说明适用于带 Webhook 上传模块的 Gadgetbridge 版本（版本号仍显示 0.93.0，
> 以"Webhook 上传"设置项存在为准）。

---

## 一、设置入口

打开 App：

1. 底部/侧边进入 **设置（Settings）**
2. 找到 **自动化（Automation）**
3. 打开 **Webhook 上传**

或者：在主界面点右上角菜单 → 设置 → 自动化 → Webhook 上传。

> 入口藏得比较深，一次找不到可以搜索设置页里的"Webhook"。

---

## 二、配置步骤（按顺序）

### 1. 开启"Webhook 上传"

把开关打开。下面的选项才会出现。

### 2. 填写服务器地址

- 只填**根地址**，例如：`health.galaxy1108.top`
- 不要加 `https://`、不要加路径，App 会自动补全为 `https://health.galaxy1108.top/upload`
- 内网自建时也可以填 `http://192.168.1.5`（需要在下面打开"允许不安全连接"）

### 3. 复制绑定码

点"**绑定码**"那一行，会把完整命令复制到剪贴板，例如：

```
/bind GB-XXXXXX
```

把这条命令发给你的机器人（微信/QQ 等）。机器人回复配对成功前，数据不会入库。
绑定按手环的 MAC 地址关联，重装 App、换绑定码都不影响已绑定设备。

> 绑定码就是门禁，没有令牌、不需要别的密钥。

### 4. 选择上传的数据类型

点"**数据类型**"勾选要上传的内容：步数、心率、睡眠、运动记录、扩展数据等。
默认全选，保持默认即可。

### 5. 设置上传间隔

"上传间隔（分钟）"默认 15 分钟。改小更实时、更费电；改大省电。

### 6. 立即上传（测试）

回到上一页打开 Webhook 上传设置，点"**立即上传**"。
上传中会显示转圈动画，完成后弹出结果：

- `Upload OK: N samples` —— 成功，N 条样本
- `Upload failed: …` —— 失败，原因见"常见问题"

### 7. 确认状态区

设置页下方有状态区：

| 项目 | 含义 |
| --- | --- |
| 配对状态 | 已配对 / 等待配对（未绑定，会提示绑定码） |
| 上次执行 | 最近一次上传时间 |
| 上次状态 | 最近一次结果，失败会附具体原因 |

---

## 三、在服务器端验证

- 发 `/devices` 查看已绑定设备及数据概况
- 发"今天走了多少步""昨晚睡眠如何""最近心率异常"等自然语言查询
- 心率、睡眠等异常会由服务器主动推送告警

---

## 四、常见问题

**上传失败：服务器没有回复 / timeout**
- 检查手机网络；服务器地址是否填对（只填根地址）
- 自建服务器没上 HTTPS 时，打开"允许不安全连接"

**配对状态一直是"等待配对"**
- 确认已把 `/bind GB-XXXXXX` 发给机器人，且机器人回复成功
- 绑定按 MAC 关联，换手机/重装后不需要重新绑定

**想重传历史数据**
- 点"重置上传游标"，再点"立即上传"会从更早的数据开始重新上传
- 积压超过 7 天时，App 会弹窗让你选"全部"或"仅最近 7 天"

**刚装新版本，运动记录查不到**
- 版本有修复（运动记录时间戳毫秒/秒问题），装新版本后：
  重置游标 → 立即上传，然后服务器再查"运动记录"即可

**通知打扰**
- 上传失败、等待配对、数据积压时会发系统通知，可在系统设置里关闭 App 通知

---
